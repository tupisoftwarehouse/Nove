import "package:flutter/widgets.dart";

class EndScreenPadding extends StatelessWidget {
  const EndScreenPadding({super.key});

  @override
  Widget build(BuildContext _) {
    return const SizedBox(height: 92, width: double.infinity);
  }
}
